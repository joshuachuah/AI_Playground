{
  "name": "ai_playground",
  "version": "0.1.0",
  "private": true,
  "description": "Foundation for a live OpenClaw-first 3D observability MVP",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit false --outDir .build",
    "validate:normalization": "npm run build -- --pretty false && node .build/dev/validate-normalization.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
